import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'uk.co.daniellowry.qrauthenticate',
  appName: 'qr-authenticate',
  webDir: 'dist',
  bundledWebRuntime: false,
};

export default config;
